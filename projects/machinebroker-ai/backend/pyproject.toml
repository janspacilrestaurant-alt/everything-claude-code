[project]
name = "machinebroker-ai-backend"
version = "0.1.0"
description = "AI-driven B2B machinery brokerage backend"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
